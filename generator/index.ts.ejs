<%_ for(let i = 0; i < moduleNames.length; i++) { _%>
import <%= moduleNames[i] %> from "./<%= moduleNames[i] %>";
<%_ } _%>

export default {
<%_ for(let i = 0; i < moduleNames.length; i++) { _%>
    <%= moduleNames[i] %>,
<%_ } _%>
}