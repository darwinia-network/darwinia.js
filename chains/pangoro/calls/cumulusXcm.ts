/**
 * This is the doc comment for pallet `CumulusXcm`'s calls. 
 * 
 * `CumulusXcm`'s storages: {@link: module:pangoro/cumulusXcm/storages}
 *
 * @module pangoro/cumulusXcm/calls
 */
import { buildRuntimeCall, Dispatch, decodeCall } from "../../../index";
import { ethers, BytesLike } from "ethers";
import { Metadata } from "@polkadot/types";

export const getCumulusXcm = (dispatch: Dispatch, metadata: Metadata) => {
    return {
    }
}

