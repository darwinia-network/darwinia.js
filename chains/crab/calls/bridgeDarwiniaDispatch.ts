/**
 * This is the doc comment for pallet `BridgeDarwiniaDispatch`'s calls. 
 * 
 * `BridgeDarwiniaDispatch`'s storages: {@link: module:crab/bridgeDarwiniaDispatch/storages}
 *
 * @module crab/bridgeDarwiniaDispatch/calls
 */
import { buildRuntimeCall, Dispatch, decodeCall } from "../../../index";
import { ethers, BytesLike } from "ethers";
import { Metadata } from "@polkadot/types";

export const getBridgeDarwiniaDispatch = (dispatch: Dispatch, metadata: Metadata) => {
    return {
    }
}

